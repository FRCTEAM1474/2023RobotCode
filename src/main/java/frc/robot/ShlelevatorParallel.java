/*package frc.robot;

import edu.wpi.first.wpilibj2.command.ParallelCommandGroup;
import frc.robot.commands.helevatorinnieandoutiecommand;
import frc.robot.commands.slelavatorinnieandoutiecommand;

public class ShlelevatorParallel {
    public class ReplaceMeParallelCommandGroup extends ParallelCommandGroup {
        // Creates a new ReplaceMeParallelCommandGroup. 
        public ReplaceMeParallelCommandGroup() {
          // Add your commands in the addCommands() call, e.g.
          // addCommands(new FooCommand(), new BarCommand());
          addCommands(new helevatorinnieandoutiecommand());
        }
      }
}
 */